import { Container } from '@mui/material';
import React from 'react';

const MyBookings = () => {
    return (
        <Container>
            My bookings
        </Container>
    );
};

export default MyBookings;